# AgentPay Environment Configuration

# BSV Network: 'testnet' or 'mainnet'
# Default: testnet (for safety)
BSV_NETWORK=testnet

# Platform Escrow Wallet (REQUIRED for production)
# Generate a secure wallet and store the private key in a KMS
# For testnet/dev: leave empty to auto-generate (ephemeral)
PLATFORM_WALLET_PRIVKEY=
PLATFORM_WALLET_ADDRESS=

# Master Encryption Key for stored private keys
# CRITICAL: Use a strong, randomly generated key in production
# Store in a secure key management system (AWS KMS, HashiCorp Vault, etc.)
AGENTPAY_MASTER_KEY=

# Database path (optional, defaults to ./data/agentpay.db)
AGENTPAY_DB=

# API Server Port
PORT=3100
